import { HeaderStickyDirective } from './header-sticky.directive';

describe('HeaderStickyDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderStickyDirective();
    expect(directive).toBeTruthy();
  });
});
